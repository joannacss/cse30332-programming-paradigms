package exceptions;

import java.io.FileInputStream;
public class CheckedExample {

    public static void main(String[] args) {
        FileInputStream fin = new FileInputStream("CheckedExample.java");
        // why is this not letting we compile our code?
        // How to fix?
        // (a) add a catch block
        // (b)
    }
}